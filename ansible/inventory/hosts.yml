all:
  children:
    k8s_control_plane:
      hosts:
        vps-k8s-demo:
          ansible_host: node01.rudolphmaier.de   # update if domain changes
          ansible_user: rudolph                   # update to your SSH user
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
